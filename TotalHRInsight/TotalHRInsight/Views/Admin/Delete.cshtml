 @model TotalHRInsight.DAL.ApplicationUser // Asegúrate de que estás pasando el modelo correcto, que sería tu entidad Usuario

@{
    ViewData["Title"] = "Desactivar Usuario";
}
<div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card border-0 shadow">
                    <div class="card-body text-center p-5">
                        <div class="mb-4">
                            <i class="bi bi-exclamation-triangle text-warning" style="font-size: 3rem;"></i>
                        </div>

                    <h2 class="card-title mb-3 text-secondary fw-bold">Desactivar Usuario</h2>
                        
                        <p class="card-text mb-4 text-muted fs-5">
                            ¿Estás seguro de que deseas desactivar a <strong class="text-dark">@Model.Nombre</strong>?
                        </p>

                        <form asp-action="Delete" method="post">
                            <input type="hidden" asp-for="Id" />
                            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                                <button type="submit" class="btn btn-danger btn-lg px-4 gap-3" data-bs-toggle="tooltip" data-bs-placement="top" title="Confirmar desactivación">
                                    <i class="bi bi-person-x me-2"></i>
                                    Desactivar
                                </button>
                                <a asp-action="Index" class="btn btn-outline-secondary btn-lg px-4" data-bs-toggle="tooltip" data-bs-placement="top" title="Volver al listado">
                                    <i class="bi bi-arrow-left me-2"></i>
                                    Cancelar
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
