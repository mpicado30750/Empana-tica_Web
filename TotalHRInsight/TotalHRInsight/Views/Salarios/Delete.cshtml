@model TotalHRInsight.DAL.Salario

@{
    ViewData["Title"] = "Eliminar Salario";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Eliminar Salario</h1>
<hr />
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4">
                            Empleado:
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.UsuarioAsignacion.Nombre) @Html.DisplayFor(model => model.UsuarioAsignacion.PrimerApellido) @Html.DisplayFor(model => model.UsuarioAsignacion.SegundoApellido)
                        </dd>
                        <dt class="col-sm-4">
                            @Html.DisplayNameFor(model => model.SalarioBruto)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.SalarioBruto)
                        </dd>
                        <dt class="col-sm-4">
                            @Html.DisplayNameFor(model => model.SalarioExtra)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.SalarioExtra)
                        </dd>
                        <dt class="col-sm-4">
                            @Html.DisplayNameFor(model => model.SalarioNeto)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.SalarioNeto)
                        </dd>
                    </dl>
                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="IdSalario" />
                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-secondary me-2" style="flex: 1;" onclick="showLoader()">
                                <i class="bi bi-arrow-left-circle"></i> Regresar
                            </a>
                            <button type="submit" class="btn btn-danger ms-2" style="flex: 1;" onclick="showLoader()">
                                <i class="bi bi-trash3-fill"></i> Eliminar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("_LoaderModal")

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
    <script src="~/js/loader.js"></script>
}
