@model IEnumerable<TotalHRInsight.DAL.Medida>
@{
    ViewData["Title"] = "Lista de Medidas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-4 text-primary mb-0">Lista de Medidas</h1>
        <a asp-action="Create" class="btn btn-primary btn-lg" onclick="showLoader()">
            <i class="bi bi-plus-circle me-2"></i>Crear Nueva Medida
        </a>
    </div>
    <hr class="mb-4" />
    <div class="row row-cols-1 row-cols-md-3 g-4" style="max-height: 70vh; overflow-y: auto;">
        @foreach (var item in Model)
        {
            <div class="col">
                <div class="card h-100 border-0 shadow-sm hover-shadow transition">
                    <div class="card-header bg-gradient bg-primary text-white py-3">
                        <h5 class="card-title mb-0 text-truncate">@item.NombreMedida</h5>
                    </div>
                    <div class="card-footer bg-light p-3">
                        <div class="d-flex justify-content-between gap-2">
                            <a asp-action="Edit" asp-route-IdMedida="@item.IdMedida"
                               class="btn btn-warning btn-sm flex-grow-1" onclick="showLoader()">
                                <i class="bi bi-pencil-square me-1"></i>Editar
                            </a>
                            <a asp-action="Details" asp-route-IdMedida="@item.IdMedida"
                               class="btn btn-info btn-sm flex-grow-1" onclick="showLoader()">
                                <i class="bi bi-info-circle me-1"></i>Detalles
                            </a>
                            <a asp-action="Delete" asp-route-IdMedida="@item.IdMedida"
                               class="btn btn-danger btn-sm flex-grow-1" onclick="showLoader()">
                                <i class="bi bi-trash me-1"></i>Eliminar
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

@Html.Partial("_LoaderModal")
@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
    <script src="~/js/loader.js"></script>
}