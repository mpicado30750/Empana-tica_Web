@model TotalHRInsight.DAL.TipoDeduccion

@{
    ViewData["Title"] = "Editar Tipo Deduccion";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1 class="text-center">Editar Tipo de Deduccion</h1>
<hr />
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="IdTipoDeduccion" />
                        <div class="form-group">
                            <label asp-for="NombreDeduccion" class="control-label"></label>
                            <input asp-for="NombreDeduccion" class="form-control" />
                            <span asp-validation-for="NombreDeduccion" class="text-danger"></span>
                        </div>
                        <form asp-action="Edit" method="post">
                            <input type="hidden" name="IdTipoDeduccion" value="@Model.IdTipoDeduccion" />
                            <div class="d-flex justify-content-between mt-4">
                                <a asp-action="Index" class="btn btn-secondary me-2" style="flex: 1;" onclick="showLoader()">
                                    <i class="bi bi-arrow-left-circle"></i> Regresar
                                </a>
                                <button type="submit" class="btn btn-danger ms-2" style="flex: 1;" onclick="showLoader()">
                                    <i class="bi bi-plus-circle"></i> Guardar
                                </button>
                            </div>
                        </form>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("_LoaderModal")

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
    <script src="~/js/loader.js"></script>
}


