@model TotalHRInsight.DAL.Gasto

@{
    ViewData["Title"] = "Editar Gasto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1 class="text-center">Editar Gasto</h1>
<hr />
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="IdGasto" />
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="IdGasto" />
                        <div class="form-group">
                            <label asp-for="Fecha" class="control-label"></label>
                            <input asp-for="Fecha" class="form-control" />
                            <span asp-validation-for="Fecha" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="TipoGastoId" class="control-label"></label>
                            <select asp-for="TipoGastoId" class="form-control" asp-items="ViewBag.TipoGastoId"></select>
                            <span asp-validation-for="TipoGastoId" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="MontoGasto" class="control-label"></label>
                            <input asp-for="MontoGasto" class="form-control" />
                            <span asp-validation-for="MontoGasto" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="CierreId" class="control-label"></label>
                            <select asp-for="CierreId" class="form-control" asp-items="ViewBag.CierreId"></select>
                            <span asp-validation-for="CierreId" class="text-danger"></span>
                        </div>
                        <form asp-action="Edit" method="post">
                            <input type="hidden" name="IdGasto" value="@Model.IdGasto" />
                            <div class="d-flex justify-content-between mt-4">
                                <a asp-action="Index" class="btn btn-secondary me-2" style="flex: 1;" onclick="showLoader()">
                                    <i class="bi bi-arrow-left-circle"></i> Regresar
                                </a>
                                <button type="submit" class="btn btn-danger ms-2" style="flex: 1;" onclick="showLoader()">
                                    <i class="bi bi-plus-circle"></i> Guardar
                                </button>
                            </div>
                        </form>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("_LoaderModal")

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
    <script src="~/js/loader.js"></script>
}