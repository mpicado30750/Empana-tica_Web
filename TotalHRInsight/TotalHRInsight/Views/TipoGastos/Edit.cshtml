@model TotalHRInsight.DAL.TipoGasto
@{
    ViewData["Title"] = "Editar Tipo de Gasto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container py-4">
    <h1 class="text-center display-4 mb-4 text-warning">Editar Tipo de Gasto</h1>
    <hr class="mb-4" />
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow-lg rounded-3">
                <div class="card-header bg-warning text-dark py-3">
                    <h5 class="card-title mb-0">Modificar Información</h5>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3"></div>
                        <input type="hidden" asp-for="IdTipoGasto" />

                        <div class="form-group mb-4">
                            <label asp-for="NombreGasto" class="form-label fw-bold"></label>
                            <input asp-for="NombreGasto" class="form-control form-control-lg" />
                            <span asp-validation-for="NombreGasto" class="text-danger small"></span>
                        </div>

                        <div class="d-grid gap-3 d-md-flex justify-content-md-between mt-4">
                            <a asp-action="Index" class="btn btn-outline-secondary btn-lg px-4 flex-grow-1" onclick="showLoader()">
                                <i class="bi bi-arrow-left-circle me-2"></i>Regresar
                            </a>
                            <button type="submit" class="btn btn-warning btn-lg px-4 flex-grow-1" onclick="showLoader()">
                                <i class="bi bi-save me-2"></i>Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("_LoaderModal")
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
    <script src="~/js/loader.js"></script>
}