@model IEnumerable<TotalHRInsight.DAL.Planilla>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .btn-inicio {
        background-color: #B42D29;
        border-color: #B42D29;
        color: #FAFCFD;
    }
</style>

<h1>Planillas</h1>

<div class="row mb-3">
    <div class="col-md-6">
        <a class="btn btn-inicio" asp-action="Create">Crear Nuevo</a>
        <a class="btn btn-inicio">Informe</a>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="thead-dark">
            <tr>
                <th>
                    Fecha de Inicio
                </th>
                <th>
                    Fecha de Fin
                </th>
                <th>
                    Monto Total
                </th>
                <th>
                    ID de Usuario de Creación
                </th>
                <th>
                    ID de Asistencia
                </th>
                <th>
                    ID de Permiso
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FechaInicio)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FechaFin)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MontoTotal)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UsuarioCrecionId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdAsistencia)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdPermiso)
                    </td>
                    <td>
                        <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@item.IdPlanilla">Editar</a>
                        <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@item.IdPlanilla">Detalles</a>
                        <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.IdPlanilla">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
