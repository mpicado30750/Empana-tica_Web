@model IEnumerable<TotalHRInsight.DAL.Sucursal>

@{
    ViewData["Title"] = "Indice Sucursal";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Sucursales</h1>

<p>
    <a asp-action="Create">Crear nuevo registro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreSucursal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UbicacionSucursal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreSucursal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UbicacionSucursal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSucursal">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdSucursal">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSucursal">Eliminar</a> |
                <a asp-action="" asp-route-id="@item.IdSucursal">Descargar</a>
            </td>
        </tr>
}
    </tbody>
</table>
