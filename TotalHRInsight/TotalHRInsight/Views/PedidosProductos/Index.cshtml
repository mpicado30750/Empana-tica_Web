@model IEnumerable<TotalHRInsight.DAL.PedidosProductos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pedido)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Medida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pedido.EstadoPedido)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PedidosProductosID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PedidosProductosID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PedidosProductosID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
